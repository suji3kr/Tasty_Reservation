
use webdb;
drop table if exists Users;
drop table if exists reservations;
drop table if exists admins;
drop table if exists restaurants;
drop table if exists store_registration;

CREATE TABLE Users (
    id bigint primary key auto_increment,
    username VARCHAR(50) NOT NULL,
    password VARCHAR(255) NOT NULL,
    age INT,
    email VARCHAR(100) NOT NULL,
    phone VARCHAR(20),
    gender ENUM('M', 'F') NOT NULL
);

CREATE TABLE Restaurants (
    restaurant_id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    location VARCHAR(255),
    phone VARCHAR(20),
    category VARCHAR(50)
);

CREATE TABLE store_registration (
    id INT AUTO_INCREMENT PRIMARY KEY,        -- 가게 고유 ID
    store_name VARCHAR(255) NOT NULL,          -- 가게 이름
    store_location VARCHAR(255) NOT NULL,      -- 가게 위치
    store_description TEXT,                    -- 가게 설명
    store_category VARCHAR(50),                -- 가게 종류
    phone_number VARCHAR(20),                  -- 전화번호
    store_image VARCHAR(255)                   -- 가게 사진 (파일 경로)
);

CREATE TABLE reservations (
    id INT AUTO_INCREMENT PRIMARY KEY,
    store_id INT NOT NULL,
    customer_name VARCHAR(255) NOT NULL,
    phone_number VARCHAR(20) NOT NULL,
    reservation_date DATE NOT NULL,
    reservation_time TIME NOT NULL,
    people_count INT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (store_id) REFERENCES store_registration(id) ON DELETE CASCADE
);d


CREATE TABLE Admins (
    admin_id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) NOT NULL,
    password VARCHAR(255) NOT NULL
);



--------------------------------------------------------------------------------------------



//루트에 아래 명령어를 입력하고 새로운 커넥션추가 (db이름 = restdb, 사용자이름 = restuser, 비밀번호 = 12345

Create database restdb;

create user 'restuser'@'localhost' identified by '12345';

grant all privileges on restdb.* to 'restuser'@'localhost';


use restdb;
drop table users;
-- Admins 테이블
CREATE TABLE Admins (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL
);

-- Users 테이블
CREATE TABLE Users (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL,
    age INT,
    email VARCHAR(100) UNIQUE,
    phone VARCHAR(20),
    gender ENUM('M', 'F')
);

-- Restaurants 테이블
CREATE TABLE Restaurants (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    admin_id bigint,
    store_name VARCHAR(255) NOT NULL,
    store_location VARCHAR(255),
    store_description TEXT,
    store_category VARCHAR(50),
    phone_number VARCHAR(20),
    store_image VARCHAR(255),
    FOREIGN KEY (admin_id) REFERENCES Admins(id) ON DELETE SET NULL
);

-- Reservations 테이블
CREATE TABLE Reservations (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    store_id bigINT,
    user_name VARCHAR(255) NOT NULL,
    phone VARCHAR(20),
    reservation_date DATE NOT NULL,
    reservation_time TIME NOT NULL,
    people_count INT NOT NULL,
    FOREIGN KEY (store_id) REFERENCES Restaurants(id) ON DELETE CASCADE
);

-- Reviews 테이블
CREATE TABLE Reviews (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    store_id bigINT NOT NULL,
    title VARCHAR(200),
    content TEXT,
    user_id BIGINT NOT NULL,
    regdate DATE NOT NULL DEFAULT (CURRENT_DATE),
    FOREIGN KEY (store_id) REFERENCES Restaurants(id) ON DELETE CASCADE,
    FOREIGN KEY (user_id) REFERENCES Users(id) ON DELETE CASCADE
);


<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
        
<mapper namespace="reservation">

    <!-- 예약 저장 -->
    <insert id="save" parameterType="com.company.domain.ReservationDTO">
        INSERT INTO reservations (store_id, customer_name, phone_number, reservation_date, reservation_time, people_count)
        VALUES (#{storeId}, #{customerName}, #{phoneNumber}, #{reservationDate}, #{reservationTime}, #{peopleCount})
    </insert>

    <!-- 특정 가게의 예약 목록 조회 -->
    <select id="findByStoreId" parameterType="int" resultType="com.company.domain.ReservationDTO">
        SELECT * FROM reservations WHERE store_id = #{storeId}
    </select>

</mapper>
